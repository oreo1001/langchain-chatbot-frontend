// /types/index.ts
export interface Board {
    id: number;
    title: string;
    content: string;
    dateTime: string;
}

export interface myComment {
    id: number;
    question: string;
    response: string;
}